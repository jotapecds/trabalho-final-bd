CREATE TABLE bairro ( 
    nome INT PRIMARY KEY, 
    qtd_beneficiarios_pbf INT, 
    qtd_extrema_popreza INT,
    qtd_ep_cadastrado INT,  
    qtd_pobreza INT, 
    qtd_baixa_renda INT, 
    qtd_acima_meio_sal_min INT
); 

CREATE TABLE patrimonio ( 
    id INT PRIMARY KEY, 
    localizacao INT,  
    id_bairro INT 
); 

CREATE TABLE esfera_do_governo. ( 
    id INT PRIMARY KEY, 
    tipo INT
);

CREATE TABLE biblioteca ( 
    acervo INT, 
    nome INT PRIMARY KEY, 
    id_bairro INT
); 

CREATE TABLE museu ( 
    id INT PRIMARY KEY, 
    endereco INT, 
    tipo INT, 
    id_bairro INT
); 

CREATE TABLE patrimonio_esfera_do_governo ( 
    id_patrimonio INT, 
    id_esfera_do_governo INT
);

ALTER TABLE Patrimônio ADD FOREIGN KEY(idBairro) REFERENCES Bairro (idBairro);
ALTER TABLE Biblioteca ADD FOREIGN KEY(idBairro) REFERENCES Bairro (idBairro);
ALTER TABLE Museu ADD FOREIGN KEY(idBairro) REFERENCES Bairro (idBairro);
ALTER TABLE Pertence ADD FOREIGN KEY(ID) REFERENCES Patrimônio (ID);
ALTER TABLE Pertence ADD FOREIGN KEY(ID) REFERENCES Esfera do Gov. (ID)